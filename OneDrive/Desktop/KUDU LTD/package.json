{
  "name": "kudu-talent",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "node scripts/check-env.mjs && vite build",
    "validate:env": "node scripts/check-env.mjs",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.3"
  }
}
